<%= form_with(model: film_list) do |form| %>
  <% if film_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(film_list.errors.count, "error") %> prohibited this film_list from being saved:</h2>

      <ul>
        <% film_list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :group_id %>
    <%= form.text_field :group_id %>
  </div>

  <div class="field">
    <%= form.label :film_id %>
    <%= form.text_field :film_id %>
  </div>

  <div class="field">
    <%= form.label :date_discussed %>
    <%= form.datetime_select :date_discussed %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
